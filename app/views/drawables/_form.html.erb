<%= form_for(@drawable) do |f| %>
  <% if @drawable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drawable.errors.count, "error") %> prohibited this drawable from being saved:</h2>

      <ul>
      <% @drawable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :score %><br />
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
